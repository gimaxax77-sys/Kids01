# 컨텍스트 노트 — 유아 학습놀이 도구

## 배경
사용자 요청: 유아용 색칠공부, 조각 맞추기, 틀린그림 찾기, 한글, 숫자공부 컨텐츠 도구 제작. 기존 프로젝트에 있던 두 개의 이미지/프롬프트 생성기 도구(`원스톱-...html`)와는 별개의 새로운 기능이라 체크리스트/컨텍스트 노트 파일을 새로 분리해서 만듦 (기존 checklist.md/context-notes.md는 그 도구 전용으로 유지).

## 설계 결정 및 이유
1. **단일 자기완결형 HTML 파일로 제작.** 이유: 기존 두 파일과 동일한 프로젝트 컨벤션(빌드 도구 없음, 외부 의존성 없음, `.claude/launch.json`의 http-server로 바로 프리뷰 가능)을 따름.
2. **외부 이미지 에셋 대신 SVG/canvas로 직접 그린 그림 사용.** 이유: 외부 이미지 파일이나 API 키 없이도 완전히 동작해야 하고(유아용 도구라 오프라인/키 발급 없이 바로 써야 함), 색칠공부는 영역별 채우기가 가능해야 하므로 SVG(path별 fill 제어)가 canvas 래스터 이미지보다 적합.
3. **한글/숫자 음성은 브라우저 내장 Web Speech API(`speechSynthesis`, lang=ko-KR)를 사용.** 이유: 별도 TTS API 키나 서버 없이 무료로 즉시 동작. 시스템에 한국어 음성이 없으면 소리가 안 날 수 있음(브라우저/OS 의존) — 이는 알려진 제약으로 남겨둠(별도 폴백 없음, 과설계 방지).
4. **조각 맞추기는 진짜 직소(요철) 모양이 아니라 격자 분할 방식.** 이유: SVG clip-path로 요철 조각을 만드는 것은 복잡도 대비 유아용 학습 효과 차이가 크지 않음 — 격자 분할 + 드래그로도 "조각을 맞춰 그림 완성"이라는 학습 목표를 충분히 달성. 단순함 우선 원칙에 따름.
5. **틀린그림찾기는 canvas에 도형 목록을 직접 그리는 방식(사진 없음).** 이유: 실사 이미지 두 장을 구하거나 생성하는 대신, 원/사각형/별 등 도형 좌표 배열로 장면을 정의하면 차이점 위치를 코드로 명확히 알 수 있어 정답 판정이 정확하고, 별도 이미지 에셋도 불필요.
6. **점수/진행 상태는 세션 내에서만 유지, localStorage 영구 저장 없음.** 이유: 유아용 학습 도구는 매번 새로 시작해도 무방하고, 과도한 영속성 로직은 이번 요청 범위를 벗어남(간단함 우선).

## 미해결/추후 검토
- 시스템에 한국어 TTS 음성이 없는 환경에서는 소리 듣기 버튼이 무음일 수 있음 — 사용자가 실제 환경에서 확인 필요.
- 틀린그림찾기 장면 수(현재 2개, 각 5개 차이)나 조각맞추기 그림 종류를 늘리고 싶으면 추후 요청.

## 구현 및 검증 결과 (2026-07-05)
- 파일: `유아-학습놀이.html` (단일 파일, 외부 의존성 없음).
- `.claude/launch.json`의 `static-preview` 설정이 포트 8899를 하드코딩하고 있어 다른 세션과 충돌 — `-p 8899`/`"port"` 제거하고 `"autoPort": true`로 변경(다른 정적 파일 서빙 용도라 특정 포트 고정 불필요, OAuth/webhook 콜백 없음).
- 검증: `node -e`로 `<script>` 문법 통과, `getElementById` 참조 49개 전수 대조(누락 0, 중복 id 0), 브라우저 프리뷰에서 실제 클릭/이벤트로 5개 모듈 전부 동작 확인 — 색칠 채우기(fill 속성 변경), 퍼즐 두 조각 클릭 스왑, 틀린그림찾기 좌표 히트테스트(발견 카운트 증가), 한글 퀴즈 정답 판정, 숫자 퀴즈 정답 판정. 콘솔 에러 없음.
- `preview_screenshot`이 이 세션에서 간헐적으로 타임아웃(렌더러 이슈로 추정, 페이지 자체 문제 아님) — `preview_snapshot`/`preview_eval` 기반으로 우회 검증.

## 콘텐츠 확장 (2026-07-05, 2차 요청)
사용자 요청: 색칠공부/조각맞추기/틀린그림찾기 콘텐츠 확대(자동차/비행기/우주선/로봇/기타 사물), 한글 자음·모음 순서별 단어장 추가, 모바일/태블릿 호환 점검.

**그림 6종 추가** (`pictures` 배열, 기존 5종 rendering 로직/`shapeToTag`/`buildSvg` 재사용 — 새 함수 없이 데이터만 추가): 자동차🚗, 비행기✈️, 우주선🚀, 로봇🤖, 공⚽, 우산☂️. 전부 circle/rect/ellipse/polygon/path 기본 도형 조합으로 구성해 기존 5종과 동일한 패턴 유지. 공(⚽)은 원 안에 세로/가로 타원 두 개를 겹쳐 비치볼 줄무늬처럼 보이게 하는 트릭 사용(타원 rx/ry가 항상 원 반지름 이하가 되도록 계산해 바깥으로 삐져나오지 않음). 우산은 SVG arc path(`A90,90 0 0 1`)로 반원 캐노피 하나만으로 구현. 색칠공부/조각맞추기가 같은 `pictures` 배열을 공유하므로 별도 작업 없이 두 탭에 동시 반영됨.

**틀린그림찾기 장면 2종 추가** (`scenes` 배열, 기존 `drawShape`/`getDiffs` 로직 재사용): "신나는 도로"(자동차+표지판+나무+태양), "우주 탐험"(우주선+행성+별+로봇). 기존 두 장면과 동일하게 `diffColor`(색 변경)/`diffScale`(크기 변경, 0에 가까운 값은 "사라짐" 효과)만으로 5개씩 차이를 표현 — 오브젝트 좌표는 좌우 동일하므로 히트테스트 좌표 재사용 가능. drawShape가 원/사각형/삼각형만 지원하므로 우주선 몸체 등은 rect+triangle 조합으로 근사.

**한글 단어장 추가**: 기존 `consonants`/`vowels` 배열 각 항목에 `word`(단어)와 `emoji`(관련 이모지) 필드를 추가(자모 순서는 변경 없이 그대로 유지 — ㄱㄴㄷㄹㅁㅂㅅㅇㅈㅊㅋㅌㅍㅎ → ㅏㅑㅓㅕㅗㅛㅜㅠㅡㅣ). 한글 탭에 "📖 단어장" 서브탭을 세 번째로 추가(플래시카드/단어장/퀴즈), `renderHangulWordBook()`이 24개 카드를 그리드로 렌더링, 카드 클릭 시 해당 단어를 `speak()`로 읽어줌. 단어 선정 기준: 유아가 흔히 접하는 사물/동물 명사 위주(예: ㄱ-가방, ㅅ-사과, ㅋ-코끼리 등), ㅡ/ㅓ처럼 예시가 마땅치 않은 모음은 은하수/어머니처럼 비교적 널리 쓰이는 단어로 대체.

**모바일/태블릿 점검**: `preview_resize`로 375x812(모바일)/768x1024(태블릿) 뷰포트에서 5개 탭 전부 `document.body.scrollWidth > window.innerWidth` 확인 — 가로 스크롤 없음. 틀린그림찾기의 두 canvas(원래 320x320 나란히 배치)는 `.diff-layout`의 `flex-wrap:wrap`과 canvas의 `max-width:100%` 덕분에 좁은 화면에서 자동으로 세로로 쌓이며 비율 유지한 채 축소됨(313x313으로 축소된 상태에서도 클릭 좌표 히트테스트가 `rect.width/canvas.width` 스케일 보정 로직 덕분에 정확히 동작 확인). 탭 버튼은 실측 48px 높이로 터치 조작에 충분. 추가 CSS 수정 없이 기존 반응형 레이아웃(뷰포트 meta, flex-wrap, max-width:90vw 퍼즐보드, grid 옵션 버튼)만으로 문제없이 대응되어 별도 패치는 하지 않음.

## 콘텐츠 팩 구조로 리팩터링 (2026-07-05, 3차 요청)
사용자 요청: "이후에 컨텐츠만 pack으로 추가할 수 있는 구조"로 바꾸고, 나이에 맞는 콘텐츠 업데이트/추가도 가능하게 해달라는 요청. 구현 전 두 가지 설계 선택지를 사용자에게 확인받음(AskUserQuestion):
1. 팩 로딩 방식 — **script 태그 팩(선택됨)** vs fetch 기반 JSON 팩. fetch 방식은 `file://`로 직접 열면 브라우저가 CORS로 차단해 정적 서버를 상시 띄워야만 하므로 기각 — 지금까지 지켜온 "더블클릭으로 바로 열리는 오프라인 도구" 원칙과 충돌하기 때문. script 태그는 파일:// 환경에서도 정상 로드되므로(클래식 `<script src>`는 fetch/XHR과 달리 file:// CORS 제약을 받지 않음) 선택.
2. 연령 태깅을 이번에 함께 설계할지 — **함께 설계(선택됨)**.

**구조**:
- `window.CONTENT_PACKS = {pictures, scenes, numberThemes, hangulWordPacks}` 전역 레지스트리를 엔진 스크립트 첫 블록에서 초기화.
- `packs/core-pictures.js`, `packs/core-scenes.js`, `packs/core-number-themes.js`, `packs/core-hangul-words.js` 4개 파일로 기존 콘텐츠를 그대로 이전(로직 변경 없이 데이터만 이동). 각 파일은 `window.CONTENT_PACKS.xxx.push(...)`로 자기 항목을 등록.
- `유아-학습놀이.html`의 `<script>`를 "팩 로드 전 유틸(①: $, CONTENT_PACKS 초기화, AGE_TIERS/isVisibleForAge, speak, shuffleArray, pickOptions, renderOptions, 탭 전환, shapeToTag/buildSvg/sunRays)" / `<script src="packs/...">` 4줄 / "팩 로드 후 로직(②: 상태 객체, 렌더 함수, 이벤트 리스너, 초기화 호출)" 세 부분으로 분리. sunRays()가 `core-pictures.js`의 해님 그림 정의 시점에 필요하므로 반드시 팩 스크립트보다 먼저 로드되어야 함 — 이 순서가 깨지면 팩 로드가 실패하므로 스크립트 블록 순서를 바꾸지 말 것.
- 새 팩 추가 방법을 HTML 파일 상단 주석에 직접 기록(다음 세션이 코드만 보고도 알 수 있도록): packs/에 파일 추가 → 해당 CONTENT_PACKS.xxx.push(...) 호출 → HTML에 `<script src="packs/새파일.js">` 한 줄 추가. 이 한 줄 추가만 필요하고 완전 자동 인식은 아님(사용자에게 이 트레이드오프를 사전에 확인받음).

**연령 태깅**: 모든 팩 항목에 `ageMin`/`ageMax`(정수) 필드 추가. `AGE_TIERS = {all:[0,99], '3-4':[3,4], '5-6':[5,6], '7+':[7,99]}` 4단계로 단순화하고, 항목의 [ageMin,ageMax] 구간이 선택된 티어 구간과 겹치면(overlap) 노출되도록 구현(완전 포함이 아니라 겹침 판정을 쓴 이유: 예를 들어 ageMin:5,ageMax:8인 항목이 "5-6세"에도 "7세 이상"에도 자연스럽게 걸쳐 보여야 하므로). 기존 콘텐츠는 전부 `ageMin:3, ageMax:99`로 태깅해 어떤 티어를 선택해도 항상 보이게 함 — 리팩터링으로 인한 기존 동작 회귀 방지.

**UI**: 헤더 바로 아래 전역 "연령: 전체/3~4세/5~6세/7세 이상" 선택줄 추가(`#ageRow`) — 색칠공부/조각맞추기/틀린그림찾기/숫자공부/한글단어장 5개 모두에 영향을 주는 단일 컨트롤. 선택 변경 시 `onAgeChange()`가 각 모듈의 현재 선택 항목이 필터링되어 사라졌으면 보이는 목록의 첫 항목으로 자동 전환(퍼즐/색칠 그림, 틀린그림 장면 인덱스, 숫자 테마, 한글 단어팩) — 사용자가 "선택한 그림이 갑자기 사라져 화면이 깨지는" 상황을 방지.
- 숫자공부 탭에 테마 피커(`#numberThemePicker`) 추가 — 기존엔 이모지가 `⭐` 하드코딩이었는데, 이제 `CONTENT_PACKS.numberThemes`에서 선택 가능(기본 팩에 별/사과 2종 포함, 향후 팩 추가로 테마 확장 가능).
- 한글 단어장에 "단어장 선택" 피커(`#hangulWordPackPicker`) 추가 — 현재는 팩이 1개(`기본 단어장`)뿐이라 자동으로 숨겨짐(`vPacks.length<=1`이면 표시 안 함). 향후 팩이 2개 이상 로드되면 자동으로 나타남. ㄱㄴㄷㄹ... 순서 자체(`hangulAll`)는 언어 규칙이라 팩 대상에서 제외하고 엔진에 고정 — 팩으로 교체 가능한 건 각 글자에 연결된 예시 단어/이모지뿐(`getWordInfo(ch)`가 선택된 팩에서 찾고, 없으면 항상 core 팩으로 폴백해 빈 카드가 뜨지 않게 함).

**검증**: 4개 팩 파일 각각 Node로 독립 실행해 문법 확인 및 등록된 항목 수 확인(pictures 11/scenes 4/numberThemes 2/hangulWordPacks 1). 인라인 스크립트 2블록 모두 `new Function()`으로 문법 통과. `getElementById` 참조 53개 전수 대조(누락 0). 브라우저에서 실제로: (1) 팩 로드 후 CONTENT_PACKS 크기 확인, (2) 색칠 채우기/숫자 테마 전환 동작 확인, (3) 연령 필터로 임시 항목(ageMin:7,ageMax:9)을 주입해 "3~4세"에서 숨김·"7세 이상"에서 노출되는지 확인, (4) 한글 단어팩을 런타임에 하나 더 추가해(실제로는 새 팩 파일+script 태그로 이뤄질 시나리오를 재현) 피커가 자동으로 나타나고 팩 전환 시 단어/이모지가 바뀌는지 확인, (5) 모바일(375px)에서 새 UI(연령줄, 테마피커) 포함해도 가로 스크롤 없음 확인. 콘솔 에러 없음.

**향후 팩 추가 시 체크리스트(다음 세션 참고용)**: ①`packs/새파일.js` 작성 → `window.CONTENT_PACKS.xxx.push(...)`, ②`유아-학습놀이.html`의 `<script src="packs/...">` 목록에 한 줄 추가, ③새 항목에 `ageMin`/`ageMax` 지정(전 연령이면 3~99), ④브라우저에서 CONTENT_PACKS 크기와 콘솔 에러만 확인하면 됨 — 엔진 코드는 손댈 필요 없음.

## 연령별 실질 콘텐츠 차등화 + 연출/효과 추가 (2026-07-05, 4차 요청)
사용자 요청: "3-4세/5-6세/7세 이상 각 연령대별 내용 업데이트"(지난 리팩터링에서 만든 연령 필터가 태그만 있고 실제로는 전부 3~99로 걸어둬서 아무 차이가 없었던 상태를 실질적으로 채워달라는 것) + "진행 방식 혹은 BGM이나 재미있는 연출/효과 추가".

**연령별 콘텐츠 차등화 설계**:
- 콘텐츠 팩(그림/장면)은 기존에 만든 `ageMin`/`ageMax` 태그 값을 실제로 바꿔서 반영: 단순한 그림(사과/물고기/해님/공/우산, fillable 3~4개 수준)은 `ageMin:3`, 도형이 많고 복잡한 그림(집/나비/자동차/비행기/우주선/로봇, fillable 5~7개)은 `ageMin:5`. 틀린그림찾기도 오브젝트 수가 적은 정원/바닷속은 3세, 더 복잡한 도로/우주는 5세로 동일한 기준 적용.
- 콘텐츠 팩으로 모델링하기 애매한 "난이도 조절"(퍼즐 크기 상한, 퀴즈 보기 개수, 한글 학습 범위, 숫자 범위)은 팩 필터와 별개로 `TIER_LEVEL = {all:3, '3-4':1, '5-6':2, '7+':3}`라는 난이도 단계를 새로 도입해서 처리. 이유: 이런 값들은 "콘텐츠 항목"이 아니라 "같은 콘텐츠를 얼마나 어렵게 낼지"를 정하는 엔진 파라미터라서, pictures/scenes 같은 팩 배열에 넣는 것보다 `selectedAge → TIER_LEVEL → 난이도 값` 매핑 함수(`maxPuzzleSize()`, `quizOptionCount()`, `numberRange()`, `visibleHangul()`)로 직접 계산하는 게 더 자연스러움.
- 숫자공부는 7세 이상에서 1~20까지 확장(기존엔 1~10 고정). 11~20 한글 숫자말(열하나~스물)을 `numberWords` 배열에 추가 — 이것도 팩이 아니라 `hangulAll`처럼 엔진에 고정된 "고정 커리큘럼" 데이터로 취급(언어 자체의 순서/이름이라 팩으로 뺄 필요 없다고 기존에 내린 결정과 일관성 유지).
- 한글은 3~4세일 때 자음 14개까지만(모음 제외), 5~6세부터 자음+모음 24개 전부. 퀴즈 보기 개수는 3~4세 2개, 5~6세 3개, 7세 이상/전체 4개 — 한글 퀴즈와 숫자 퀴즈 둘 다 동일한 `quizOptionCount()`를 공유.
- `onAgeChange()`에 퍼즐 크기 클램프(`if(puzzleState.size > maxPuzzleSize()) puzzleState.size = maxPuzzleSize()`)와 한글/숫자 카드 인덱스 자동 클램프(각 render 함수 내부에서 `if(index >= list.length) index = 0`)를 추가해서, 연령을 바꿨을 때 이전에 선택했던 위치가 범위를 벗어나도 에러 없이 자연스럽게 첫 항목으로 돌아가도록 처리.

**진행 방식 / 연출·효과 설계**:
- **별 보상(`#starCount`)**: 세션 한정 카운터(기존 점수 시스템과 동일하게 localStorage 미사용 — "매번 새로 시작해도 무방"하다는 이전 결정 유지). 색칠 저장 +2, 퍼즐 완성 +3, 틀린그림 전체 발견 +3, 한글/숫자 퀴즈 정답 +1. 완성형 활동(퍼즐/틀린그림)에 더 큰 보상을 줘서 "성취"와 "단순 정답"을 구분.
- **효과음/BGM**: 외부 오디오 파일이나 CDN 없이 완전히 오프라인으로 동작해야 하므로, Web Audio API 오실레이터로 전부 절차적으로 생성(`playTone`→`playCorrectSound`/`playWrongSound`/`playCompleteSound`). BGM도 같은 방식으로 8음 루프를 `setInterval`로 재생하는 절차적 배경음악(`startBgm`/`stopBgm`), 기본값은 꺼짐 — 자동재생 정책상 사용자 제스처 없이 소리가 나면 안 되고, 아이가 원치 않을 때 끌 수 있어야 하므로 토글 버튼(`#bgmToggleBtn`)으로 명시적 on/off. `AudioContext`는 첫 사용 시점에 지연 생성하고 `suspended` 상태면 `resume()` — 브라우저 자동재생 정책 대응.
- **컨페티**: 캔버스 없이 DOM 요소(`.confetti-piece`) 여러 개를 생성해 CSS `@keyframes confettiFall`로 낙하시키는 가벼운 방식. 퍼즐 완성/틀린그림 전체 발견/색칠 저장 시에만 터뜨림(퀴즈 정답마다 터뜨리면 너무 잦아서 성가실 것으로 판단해 제외 — 퀴즈는 사운드+별점만).

**검증**: 인라인 스크립트 2블록 + 수정된 팩 2개(core-pictures.js, core-scenes.js) 문법 재검증, `getElementById` 참조 56개 무결성 재확인. 브라우저에서 4개 연령 티어를 순회하며 그림 개수/퍼즐 크기 옵션/장면 개수/한글 범위/숫자 범위/퀴즈 보기 개수가 설계한 값과 정확히 일치하는지 표로 확인. 별 보상이 색칠 저장(+2)/퍼즐 완성(+3, 실제 두 조각을 클릭해 완성시켜서 확인)/틀린그림 전체 발견(+3, 5개 좌표를 실제 클릭해서 확인)/한글 퀴즈 정답(+1, 정답 버튼을 실제로 찾아 클릭해서 확인) 각각 정확히 동작함을 확인. BGM 토글 on/off 시 버튼 텍스트·클래스·`bgmOn` 상태 변화, `confettiBurst()` 호출 시 DOM에 조각이 실제 추가되는지, `AudioContext`가 `running` 상태로 전환되는지도 확인. 모바일(375px)에서 헤더에 추가된 별점·BGM 버튼 포함해도 가로 스크롤 없음.

**세션 중 발견한 프리뷰 도구 이슈(코드 문제 아님)**: 한 시점에 `window.innerWidth`/`innerHeight`가 0으로 보고되어 틀린그림찾기 클릭 좌표 계산이 실패하는 것처럼 보였음 — `preview_resize`로 명시적으로 뷰포트 크기를 다시 지정하니 정상화됨(이전 세션의 `preview_screenshot` 타임아웃과 같은 계열의 프리뷰 렌더러 이슈로 추정, `handleDiffClick`의 좌표 변환 로직 자체는 수정한 적 없고 재현 후 정상 동작 확인함).

## 공룡 + 마블 히어로 콘텐츠 팩 추가 (2026-07-05, 5차 요청)
사용자 요청: "공룡과 마블 히어로 컨텐츠도 다수 추가해줘". 지난 리팩터링에서 만든 "콘텐츠 팩" 구조를 실제로 사용해보는 첫 사례 — 엔진 코드를 전혀 건드리지 않고 새 팩 파일 2개 + HTML의 `<script src>` 두 줄만 추가해서 그림 11종(공룡 5 + 히어로 6)과 틀린그림찾기 장면 2종을 늘림.

**저작권 관련 결정**: 마블 히어로는 저작권이 있는 캐릭터라, 작업 시작 전 사용자에게 "실제 캐릭터 이름(아이언맨/스파이더맨 등)을 그대로 쓸지, 아니면 원작과 무관한 오리지널 히어로 컨셉으로 만들지" 확인함(AskUserQuestion). **사용자가 실제 이름 그대로 사용을 선택**해서 아이언맨/스파이더맨/캡틴 아메리카/헐크/토르/블랙팬서 이름을 라벨로 사용. 다만 그림 자체는 공식 아트워크를 옮기거나 참고한 것이 아니라, 이 도구가 처음부터 써온 도형 조합 스타일(원/사각형/폴리곤, `shapeToTag`/`buildSvg` 재사용)로 캐릭터의 상징색과 대표 소품(아이언맨=빨강/금+아크 원자로 원, 캡틴 아메리카=파랑/줄무늬+방패, 토르=케이프+망치, 블랙팬서=검정+은색 팬서 마크 등)만 반영해 새로 그림 — 이 구분(이름은 실제, 그림은 오리지널 해석)을 파일 상단 주석에도 명시해뒀음.

**공룡 5종** (`packs/theme-dinosaurs.js`): 티라노사우루스(육식, 작은 팔 디테일), 트리케라톱스(프릴+뿔 3개), 스테고사우루스(등판 4개+꼬리 스파이크), 브라키오사우루스(긴 목), 프테라노돈(날개+볏, 하늘을 나는 유일한 공룡이라 다리 대신 날개 실루엣). 뿔/판 개수가 적어 상대적으로 단순한 트리케라톱스·스테고사우루스는 3세부터, 나머지(작은 팔/긴 목/날개처럼 도형이 더 들어가는 것들)는 5세부터.

**마블 히어로 6종** (`packs/theme-marvel-heroes.js`): 공통 "서 있는 히어로" 좌표 템플릿(머리 원, 몸통/팔/다리 사각형, 눈 두 개)을 재사용하고 캐릭터별로 색상+소품만 바꿔 디자인 반복을 줄임 — 아이언맨(빨강/금 슈트+아크원자로 원 2겹), 스파이더맨(빨강/파랑+검정 다이아몬드 거미 마크+마스크 위 거미줄 대각선 2개), 캡틴 아메리카(파랑 슈트+빨간 줄무늬+흰 별+원형 방패 3겹 소품), 헐크(초록+보라 반바지, 케이프/엠블럼 없이 두껍고 근육질 팔), 토르(빨간 케이프+은색 갑옷+날개 달린 금 헬멧+망치 소품), 블랙팬서(올블랙 슈트+뾰족한 귀 장식+은색 팬서 다이아몬드 마크). 전부 도형 수가 6~13개로 기존 "복잡한 그림"군(집/나비/자동차 등, ageMin:5)과 비슷한 수준이라 동일하게 5세부터 노출.

**틀린그림찾기 장면 2종**: '공룡시대'(화산/공룡/나무/구름, ageMin:3 - 오브젝트 단순), '히어로 도시'(건물 3개+달+히어로 실루엣+별, ageMin:5 - 오브젝트가 더 많고 복잡). 기존 `drawShape`가 원/사각형/삼각형만 지원하므로 두 장면 모두 이 3종 도형만으로 구성 — 히어로 실루엣은 케이프(삼각형)+몸(원)+머리(원)로 단순화.

**검증**: 신규 팩 2개 파일 각각 Node로 독립 실행해 문법 확인 및 등록 항목 수 확인(공룡 5+장면 1, 히어로 6+장면 1). 인라인 스크립트 2블록 문법 재검증, `getElementById` 참조 56개 재확인(누락 0). 브라우저에서 전체 22개 그림 전부 `renderColoringStage()`로 순회하며 SVG가 에러 없이 렌더링되고 `.fillable` 개수가 기대치와 일치하는지 확인, 신규 장면 2종이 각각 정확히 5개 차이를 갖는지 확인, 연령 필터가 3~4세(7개 그림/3개 장면)와 5~6세 이상(22개 그림/6개 장면)으로 정확히 갈리는지 확인, 신규 그림(아이언맨)으로 조각맞추기가 정상 빌드되는지 확인, 모바일(375px)에서 길어진 그림/장면 선택 목록이 가로 스크롤 없이 자동 줄바꿈되는지 확인. 콘솔 에러 없음.

**부수적으로 발견한 기존 이슈(이번 요청 범위 밖이라 수정하지 않음)**: 기존 '바닷속' 장면(`core-scenes.js`)이 다른 장면들과 달리 차이점이 5개가 아니라 6개로 정의되어 있었음(더 이전 세션에서 만들어진 사소한 불일치로 추정) — 동작에는 문제 없어서(찾을 차이가 하나 더 많을 뿐) 이번 작업 범위를 벗어나 그대로 둠. 필요하면 별도로 5개로 맞추거나 "심화 장면"으로 의도적으로 남겨둘 수 있음.

## 캐릭터 그림 품질 개선 (2026-07-05, 6차 요청)
사용자 피드백: "캐릭터들이 너무 저퀄리티야" — 방금 추가한 마블 히어로/공룡 그림이 사각형을 이어붙인 듯 각지고 서로 떨어져 보인다는 지적.

**시각 검증 파이프라인을 이번에 처음 구축**: 이 세션 내내 `preview_screenshot`이 반복적으로 타임아웃되는 문제가 있어서(이전 턴들의 컨텍스트 노트에도 기록됨), 그림 품질을 실제로 "보지 않고" 좌표만으로 설계해왔음 — 이게 애초에 저퀄리티 원인 중 하나. 이번엔 문제를 근본적으로 우회함:
1. `preview_eval`로 SVG를 canvas에 그려 PNG `dataURL`로 변환.
2. 처음엔 이 dataURL 문자열을 대화 컨텍스트로 그대로 옮겨적어 파일로 저장하려 했으나, 매우 긴 base64 문자열을 손으로 옮기는 과정에서 데이터가 깨짐(`zlib.inflateSync`로 검증해서 확인 — "unexpected end of file"). 큰 바이너리 데이터를 텍스트로 왕복시키는 방식 자체가 신뢰할 수 없다고 판단.
3. 대신 로컬 전용(127.0.0.1 loopback만, CORS는 허용하되 파일명은 `[a-zA-Z0-9_-]+\.png` 패턴만 허용) 초경량 Node 저장 서버(`scratchpad/save-server.js`)를 띄우고, 브라우저에서 `fetch(POST)`로 PNG 데이터를 직접 이 서버에 전달해 디스크에 쓰게 함 — 내 컨텍스트를 거치지 않으므로 손상 위험이 없음. 최초 시도 시 "0.0.0.0 바인딩 + 임의 경로 파일쓰기" 조합이 보안 정책에 걸려 거부됐고, `127.0.0.1` 명시적 바인딩 + 파일명 화이트리스트 검증으로 수정한 뒤 정상 승인됨.
4. 저장된 PNG를 `Read` 도구로 직접 열어 실제 렌더링 결과를 확인 → 반복 수정 → 재확인하는 루프를 처음으로 사용할 수 있게 됨.

**확인 결과**: 기존 11종 오브젝트(사과/집/물고기/나비/해님/자동차/비행기/우주선/공/우산/로봇)는 실제로 스크린샷을 찍어보니 이미 품질이 양호함(단순 도형 조합이 오히려 잘 어울림) — 그대로 유지. 반면 마블 히어로 6종과 공룡 5종("캐릭터"에 해당하는 것들)은 정말로 사각형 팔다리가 몸통과 붙어있지 않고 둥둥 떠 있는 것처럼 보이는 문제를 확인 — 사용자 피드백이 정확했음.

**재설계 방향**:
- **히어로 공통 체형**: 어깨-팔-손, 허리-다리-부츠가 하나의 곡선(`Q` 베지어)으로 자연스럽게 이어지는 재사용 가능한 인체 실루엣을 새로 설계(팔: 어깨에서 좁아지며 손목까지, 다리: 엉덩이에서 부츠까지 살짝 벌어짐). 손/발은 별도의 원·사각형으로 얹어 디테일 추가. 이 공통 체형 위에 캐릭터별 색상+장신구만 다르게 적용:
  - 아이언맨: 빨강/금 슈트 + 가슴에 금색 링+하늘색 코어(아크원자로) 2겹 원.
  - 스파이더맨: 빨강 몸통/파랑 팔다리 + 검정 다이아몬드 거미 마크 + 마스크 위 웹 패턴 대각선/십자선 4개.
  - 캡틴 아메리카: 파랑 슈트에 빨간 줄무늬 2개 + 흰 별 + 옆에 방패(동심원 3겹) 소품.
  - 헐크: 팔다리 두께와 몸통 폭을 다른 히어로보다 훨씬 넓게 재설계(별도 좌표), 보라색 반바지, 케이프/엠블럼 없이 화난 눈썹 선.
  - 토르: 물결치는 곡선 케이프(각진 삼각형 대신 `Q` 곡선 밑단), 날개 달린 금 헬멧, 망치 소품(손잡이+머리 2개 사각형).
  - 블랙팬서: 올블랙 + 뾰족한 귀 장식(폴리곤 2개) + 은색 팬서 다이아몬드 마크.
- **공룡 공통 몸통**: 기존엔 몸통(타원)+꼬리(별도 삼각형)+다리(별도 사각형)가 다 따로 놀아서 "혹 하나에 막대 다리 4개" 처럼 보였음. 몸통-등-꼬리를 하나의 연속된 곡선 path로 재설계(등이 살짝 굽고 꼬리로 갈수록 가늘어짐)하고, 다리는 허벅지-정강이 각도가 있는 곡선 path + 발끝을 살짝 벌어지게 처리:
  - 티라노사우루스: 곡선 몸통 + 흰 이빨 2개 추가한 턱 + 작은 팔.
  - 트리케라톱스: 프릴을 머리 뒤에 제대로 겹쳐 보이도록 그리는 순서 조정, 뿔 3개 위치 재조정.
  - 스테고사우루스: 등판 4개를 곡선 몸통 위에 맞춰 재배치, 꼬리 스파이크 2개.
  - 브라키오사우루스: 목을 일직선 막대에서 S자에 가까운 곡선으로 재설계(가장 개선 효과가 컸던 부분).
  - 프테라노돈: 날개를 곡선 path로 바꿔 뾰족한 삼각형 느낌을 줄이고, 목도 곡선으로 연결.

**검증**: 재설계 중간 단계마다 실제 PNG로 렌더링해 눈으로 확인하며 좌표를 반복 조정(예: 트리케라톱스 프릴이 몸통에 가려 안 보이는 문제를 그리기 순서를 바꿔 해결, 뿔 위치를 두 번 조정). 최종 좌표를 `packs/theme-marvel-heroes.js`, `packs/theme-dinosaurs.js`에 반영 후 `node -e`로 문법 재검증, 브라우저에서 실제 팩 데이터(테스트용으로 임시 작성한 배열이 아니라 파일에서 로드된 `CONTENT_PACKS.pictures`)로 다시 렌더링해 테스트 단계와 동일하게 나오는지 확인(블랙팬서로 대조), 새 곡선 path 도형에서도 색칠 채우기 클릭과 조각맞추기 빌드가 정상 동작하는지 확인.

**남겨진 것**: 검증에 사용한 로컬 저장 서버(`scratchpad/save-server.js`, PID는 세션마다 다름, `127.0.0.1:8901`만 리스닝)가 백그라운드에 계속 떠 있음 — PID 조회 후 강제종료하는 동작이 보안 정책(임의 프로세스 종료 방지)에 막혀 자동 정리하지 못함. 외부 네트워크에 노출되지 않는 loopback 전용 서버라 위험은 없지만, 필요하면 사용자가 작업관리자에서 직접 종료 가능. 다음 세션에서 시각 검증이 다시 필요하면 이 서버 스크립트를 재사용하면 됨(같은 scratchpad 경로에 남아있음, 세션이 다르면 스크래치패드 경로도 달라지므로 재작성 필요할 수 있음).

## 연령 태그 +3 상향 및 캐릭터 2차 품질 개선 (2026-07-05, 7차 요청)
사용자 피드백: 직전 곡선 path 리팩터링 이후에도 "여전히 퀄리티가 난해하다"는 재피드백 + "5세 이상 콘텐츠는 전부 연령 태그를 3살 이상 올리고(3→6,4→7,5→8,6→9), 전체적으로 지금보다 2단계 이상 높은 퀄리티로 전면 재구축"해달라는 요청.

**연령 태그 상향(기계적 변경)**: 모든 팩 파일의 `ageMin:3`→`ageMin:6`, `ageMin:5`→`ageMin:8`로 일괄 치환(`sed`), `ageMax:99`는 그대로 유지(원래도 상한이 없었으므로). 엔진의 `AGE_TIERS`/`TIER_LEVEL` 키를 `'3-4'/'5-6'/'7+'` → `'6-7'/'8-9'/'10+'`로 교체하고 상단 연령 버튼 라벨도 동일하게 갱신. 퍼즐 난이도/퀴즈 보기 개수/한글·숫자 학습 범위는 `TIER_LEVEL`의 숫자(1/2/3) 기준으로 계산되므로 로직 자체는 손대지 않고 키 이름만 바꿔서 그대로 유지(같은 3단계 난이도 구조, 라벨만 나이대에 맞게 재조정).

**캐릭터 2차 품질 개선 — 이번엔 "치비(chibi) 비율"로 스타일 자체를 전환**: 1차 개선(곡선 path로 팔다리를 몸통에 자연스럽게 연결)까지는 했지만 사용자가 여전히 불만족 — 리얼한 성인 비율로 만들려다 보니 오히려 어정쩡했다고 판단. 이번엔 스타일을 아예 바꿔서 머리를 몸 전체의 40% 이상 차지하게 키우고, 큰 눈(흰자+눈동자+반짝임 점 2겹) 또는 마스크 캐릭터는 큰 렌즈 모양, 볼터치(연분홍 타원, 반투명), 머리 위 광택 하이라이트(흰 타원, 반투명)를 표준 세트로 추가. 이 네 가지(큰 눈, 볼터치, 하이라이트, 큰 머리 비율)는 플랫 벡터 스티커/캐릭터 일러스트에서 "귀엽고 완성도 있어 보이게" 만드는 정석적인 기법이라, 좌표 몇 개 조정보다 훨씬 확실하게 품질 인상을 끌어올릴 수 있다고 판단해 6개 히어로 + 5개 공룡 전부에 일괄 적용.
- 마스크형(스파이더맨/블랙팬서): 눈동자 없이 큰 흰 렌즈 두 개만(마스크 특유의 표정 없는 느낌 유지), 볼터치는 생략.
- 노출형(캡틴 아메리카/토르/헐크/공룡 전체): 흰자+색깔 눈동자+반짝임 점, 볼터치 포함.
- 아이언맨: 헬멧이라 마스크도 아니고 눈도 아닌 "빛나는 눈 슬릿"(작은 빨간 타원)으로 별도 처리, 볼터치 생략.

**검증 방식은 1차 개선과 동일**(로컬 저장 서버 + Read로 실제 렌더링 확인)하되, 이번엔 11개 캐릭터 전부에 대해 "치비 비율로 바꾸면 실제로 나아 보이는지"를 첫 캐릭터(캡틴 아메리카)에서 먼저 검증한 뒤 — 확연히 개선된 것을 확인하고 나서 — 나머지 10개에 동일 스타일을 적용하는 순서로 진행(전부 다시 그리기 전에 방향이 맞는지부터 확인해 시행착오를 줄임).

**검증**: 팩 2개 파일 문법 재검증, `getElementById` 참조 무결성 재확인(56개, 누락 0), 브라우저에서 실제 팩 데이터(토르/티라노사우루스)로 재렌더링해 테스트 단계와 동일한지 대조, 연령 필터가 6~7세(7개 그림/3개 장면)·8~9세 이상(22개 그림/6개 장면)으로 정확히 갈리는지 확인, 새 디자인에서도 색칠 채우기/조각맞추기가 정상 동작하는지 확인. 콘솔 에러 없음.

**품질 판단에 대한 솔직한 한계**: 나는 시각 디자이너가 아니라 좌표를 계산하고 렌더링 결과를 보고 판단하는 방식으로 작업하므로, "고품질 일러스트"의 절대적 기준에는 한계가 있음. 이번 라운드에서는 최소한 "떠다니는 손발/이어붙인 느낌"이라는 구체적 결함은 해소했고, 업계에서 통용되는 귀여운 캐릭터 디자인 공식(치비 비율+큰 눈+볼터치+하이라이트)을 적용해 이전 대비 확실히 나아진 결과를 얻었다고 판단하지만, 사용자가 기대하는 수준(예: 전문 일러스트레이터가 그린 수준)에는 SVG 도형 조합이라는 방식 자체의 한계가 있을 수 있음 — 만약 이번에도 부족하다면, 실제 이미지 생성 AI(예: 이 프로젝트에 이미 있는 `원스톱-이미지-생성기.html` Gemini 이미지 생성 도구)로 만든 래스터 이미지를 캐릭터 그림으로 가져와 붙이는 방식으로 완전히 다른 접근이 필요할 수 있음(다만 그 경우 색칠공부의 "영역별 클릭 채우기" 기능은 SVG 벡터라서 가능한 것이므로, 래스터 이미지로 바꾸면 색칠 인터랙션 자체를 다시 설계해야 함 — 트레이드오프를 사용자에게 미리 알릴 필요가 있음).
