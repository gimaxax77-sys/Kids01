# 작업 규칙 — Kids01 (학습놀이 놀이방)

> **공통 규칙은 `D:\.CODE\AXdata\CLAUDE.md` 한 곳에만 둡니다.**
> 소통 규칙 · 작업 방식 14원칙 · 답변·기록 규칙은 그 문서를 따르며, 여기서는 반복하지 않습니다.
> ⚠ **이 폴더는 `AXdata` 하위가 아니라 그 문서가 자동으로 읽히지 않습니다. 세션 시작 시 직접 열어 읽으십시오.**
> 호출 명령 `서브`(서브에이전트·승인 게이트 필수) · `롣/로드/ㄹㄷ`(클로드 직접) · `멤/mem`(기억 검색·기록)은
> 전역 `D:\.CODE\.Claude\CLAUDE.md`에 있습니다(이쪽은 자동 로드).
>
> 프로젝트 현황은 **`HANDOVER.md`**, 결정·조사 이력은 **`research.md`** 에 있습니다.

## 이 프로젝트 전용 규칙

1. **작업이 끝나면 `git push`까지 합니다.**
   이 프로젝트는 **push가 곧 배포**입니다 — GitHub Pages가 1~2분 뒤 자동 반영합니다
   (라이브 https://gimaxax77-sys.github.io/Kids01/ · 저장소 `gimaxax77-sys/Kids01`, `main`).
   커밋만 하고 멈추면 **사용자에게 나가지 않습니다.** (Gim이 "푸시하지 마"라고 한 경우는 예외.)

2. **테스트 — 자동화된 테스트 스위트가 없습니다.** 빌드 도구 없는 순수 정적 HTML/CSS/JS라 아래로 확인합니다.
   - 구문 검사: `node --check 파일.js`
   - 동작 확인: Node로 정적 서버를 띄워 브라우저에서 확인 (이 PC에 python3 없음)
   - 공용 스크립트(`bgm.js` · `level.js`)를 건드리면 **놀이 21개 전체에 영향**이 갑니다. 대표 2~3개는 반드시 열어 봅니다.
