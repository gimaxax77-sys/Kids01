# 인수인계 (Kids01 · 학습놀이 놀이방)

> 다음 작업자(또는 다음 세션)가 맥락을 다시 추론하지 않고 바로 이어받기 위한 문서입니다.
> 상세 결정·조사 이력은 `research.md`에 누적되어 있습니다.

## 1. 한 줄 요약
유아용 자유탐색·학습 놀이 모음 웹앱(PWA). 순수 정적 HTML/CSS/JS, 빌드 도구 없음. GitHub Pages로 배포.

## 2. 링크·환경
- **라이브**: https://gimaxax77-sys.github.io/Kids01/
- **저장소**: https://github.com/gimaxax77-sys/Kids01 (public, `main`)
- **로컬**: `D:\.CODE\학습놀이_유아`
- **배포 방법**: `git push` → 1~2분 뒤 자동 반영. 별도 서버·PC 상시가동 불필요(GitHub이 호스팅).
- **테스트**: python3 없음. Node로 정적 서버 띄워 브라우저로 확인. `node --check 파일.js`로 구문검사.

## 3. 구조
- `놀이방.html` = 홈(21개 놀이 타일). `놀이방.js`가 나이 배지·이름표·정렬·즐겨찾기(⚙️)·BGM 담당.
- 각 놀이 = `이름.html` + `이름.js`(외부 JS). 좌상단 🏠로 홈 복귀.
- **공용 스크립트**(모든 놀이 포함): `bgm.js`(BGM+상단 제목), `level.js`(난이도 −/Lv/+ 스테퍼).
- `sw.js` = 서비스워커(오프라인 캐시). `manifest.json`/`icons/` = PWA.
- `packs/` = 이미지 매니페스트(자동생성, 직접수정 금지). `images/`·`color-pages/` 등 에셋.

## 4. 놀이 목록 (21개)
그림판, 소리놀이, 분류놀이, 색칠놀이 / 퍼즐, 숫자놀이, 그림자놀이, 선그리기, 붓기놀이, 짝맞추기, 패턴잇기, 미로놀이, 틀린그림, **지렁이놀이**(+도전) / 코딩놀이, 한글놀이, 셈놀이, 시계놀이, 알파벳, **화살표탈출**(+도전)
- **도전 모드**: 화살표탈출·지렁이는 `?mode=pro`로 별도 타일(어두운 색). 목숨·큰 격자 등 하드코어판.

## 5. ⚠️ 반드시 지킬 규칙 (실수 방지)
1. **반응형 크기 계산** — 캔버스/보드 = `min(clientW-좌우padding, clientH-상하padding)`. clientWidth엔 padding 포함되므로 **꼭 빼야** 태블릿/폰 가로에서 안 잘림.
2. **flex 자식엔 `min-height:0; min-width:0`** — 안 주면 캔버스 고유크기가 최소크기가 되어 화면 밖 넘침.
3. **vw 폰트/칸엔 상한** — `min(Nvw, Mpx, Kvh)`. (태블릿·가로 넘침 방지)
4. **조작은 `pointerdown`**(click 금지) — 반응 즉시.
5. **놀이 파일·공용JS 수정 후 반드시**:
   - `sw.js`의 `const CACHE = 'kids01-vN'` **버전 올리기** (+ 새 파일은 CORE 배열에 추가).
   - `놀이방.js` 수정 시 `놀이방.html`의 `놀이방.js?v=N` 올리기.
   - (안 올리면 기기 캐시로 업데이트가 안 보임. sw는 네트워크우선이지만 확실히 하려면 버전업.)
6. **새 놀이 추가 절차**: `이름.html`+`이름.js` 작성 → 홈에 `<a class="toy">` 타일 → `놀이방.js`의 `AGES` 배열에 나이 추가(타일 DOM 순서와 1:1) → sw CORE·CACHE 갱신 → 커밋·푸시.
7. **파일 첫 줄 한글 헤더 주석**, 작업 후 `git add/commit/push`까지.

## 6. "업데이트가 안 보여요" 대처 (자주 나옴)
거의 항상 **기기 캐시**. 배포 자체는 정상. 안드로이드 크롬: 주소창 자물쇠 🔒 → 사이트 설정 → 데이터 삭제 → 재접속. 확인은 시크릿 탭.

## 7. 현재 상태 (2026-07-20 기준)
- 최신 커밋: 전 놀이 화면 잘림/반응속도 일괄 점검(21놀이×4화면 통과). sw `kids01-v23`, `놀이방.js?v=11`.
- 화살표탈출: 꺾인 화살표·형상 다양화·촘촘 채움. 도전 레벨 1~50(격자 최대 24×24), 10단계부터 색 옅어짐·20단계 단색, 클리어 폭죽+팡파레.
- 지렁이: 60fps 보간 + 도전 모드(벽 사망·목숨·최고점). 도전 속도는 유아의 절반.

## 8. 열린 아이디어 (미확정)
- 화살표 색 원작처럼 단색 통일 옵션, 형상 추가(하트·별), 캐릭터 이미지(저작권상 개인 업로드만 가능).
- 도전 모드 타이머·별점 등 추가 난이도 요소.
