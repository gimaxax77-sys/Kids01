# 체크리스트 — Gemini 자동 이미지 생성 도구 (원스톱-프롬프트-생성기.html 재설계)

## 준비
- [x] 원본 HTML을 모바일용 사본으로 보존 (`원스톱-프롬프트-생성기(모바일-아이디어용).html`)
- [x] Gemini 이미지 생성 API 스펙 조사 (모델 ID, 요청/응답 형식)
- [x] 설계 결정 기록 (context-notes.md)

## UI 개편
- [x] 상단 안내(사용 순서) 문구를 "이미지 자동 생성" 흐름에 맞게 수정
- [x] STEP 1의 미드저니/GPT 플랫폼 탭 제거
- [x] "Gemini 이미지 설정" 패널 추가: 모델 선택(flash-lite/flash/pro), 종횡비, 해상도, 참조 이미지 파일 업로드
- [x] 참조 이미지 미리보기(썸네일) 표시
- [x] STEP 3 결과 영역을 텍스트 박스 대신 이미지 미리보기 + 다운로드/폴더저장/다시생성 버튼으로 교체
- [x] STEP 2 배치 결과를 이미지 그리드(갤러리)로 교체, 항목별 진행 상태 표시(대기/생성중/완료/실패)
- [x] 배치 진행률 표시(텍스트 기반) + 중단(취소) 버튼 추가
- [x] 사용량 표시에 "이미지 생성 횟수" 별도 카운터 추가

## 로직
- [x] `extractGeminiImage(data)` 헬퍼: candidates[0].content.parts에서 inlineData(base64) 추출, 실패 시 명확한 에러
- [x] 참조 이미지 파일 업로드 → FileReader로 base64 변환 → 요청 시 inlineData part로 포함
- [x] 단일 이미지 생성 함수: 캐릭터시트+아이디어+스타일 → (기존 프롬프트 엔지니어링 재사용) → 이미지 모델 호출 → base64 → Blob/ObjectURL로 렌더링
- [x] 배치 이미지 생성: 선택된 표정/제스처/사물 조합을 순차 반복 호출, 항목별 결과를 즉시 렌더링(전체 완료를 기다리지 않음)
- [x] 배치 항목별 "다시 생성" 버튼 추가 (실패/재시도 대응, 성공 항목 재생성 시 히스토리에 단일 항목으로 별도 기록)
- [x] 히스토리에는 이미지 바이트 대신 메타데이터(라벨/프롬프트/시간)만 저장하도록 확인
- [x] 폴더 저장: 배치 시작 시 디렉터리 핸들 1회 획득 후 각 이미지 저장에 재사용 (기존 `saveBlobToCharacterFolder` 패턴 확장)
- [x] 폴더 선택 미지원 환경(모바일 등)에서는 개별 다운로드 링크로 폴백
- [x] Etsy 상품 설명 생성 기능은 변경 없이 유지 확인
- [x] 프리셋 저장/불러오기 필드를 imageModel/imgAspect/imgSize로 교체

## 검증
- [x] `node -e`로 스크립트 문법 검증
- [x] getElementById 참조 무결성 검증 (스크립트 전수 조사, 누락 없음)
- [x] 정적 서버(http-server) + 브라우저 프리뷰로 실제 로드 확인 — 콘솔 에러 없음
- [x] STEP1 생성/STEP2 배치 생성 버튼 클릭 시 (키 없음) 에러 처리 정상 동작 확인
- [x] 커스텀 스타일 빈칸 방어(styleMissing) 동작 확인
- [x] 프리셋 저장 → 값 변경 → 불러오기로 원복되는지 확인
- [x] 제작 현황판 카운트(24/22/20)가 정확히 표시되는지 확인
- [ ] 브라우저에서 실제 Gemini API 키로 단일 생성 테스트 (실제 키 필요 — 사용자 확인 필요)
- [ ] 실제 배치 생성 시 중간 실패해도 이전 결과가 유지되는지 확인 (실제 키 필요)
- [ ] localStorage 용량이 이미지로 인해 초과되지 않는지 장기 사용 확인 (실제 키 필요)
