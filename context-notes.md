# 컨텍스트 노트 — Gemini 자동 이미지 생성 도구

## 배경
기존 `원스톱-프롬프트-생성기.html`(캐릭터 시트 + MJ/GPT용 프롬프트 텍스트만 생성)을 두 갈래로 나눔.
- `원스톱-프롬프트-생성기(모바일-아이디어용).html` — 원본 그대로 보존, 모바일에서 아이디어/프롬프트 텍스트 생성용으로 계속 사용.
- `원스톱-프롬프트-생성기.html`(메인 파일) — 이번 작업에서 "Gemini로 실제 이미지까지 자동 생성"하는 도구로 재설계.

미드저니 연동(설계 B)은 보류. 이유: 미드저니는 공식 공개 API가 없고, Discord 자동화는 셀프봇 방식이라 Discord 자체 ToS(셀프봇 금지)와 충돌 소지가 있음. PiAPI 등 비공식 프록시도 결국 같은 문제를 대행하는 것이라 동일한 리스크. 나중에 필요하면 "반자동 어시스턴트"(프롬프트 큐 + 클립보드 복사, 실제 전송은 사용자가 수동)로 별도 설계.

## 기술 조사 결과 (2026-07-04 기준, WebSearch/WebFetch로 확인)
- Gemini의 `generateContent` 엔드포인트는 "Legacy"로 표시되지만 여전히 정상 동작함 (신규 권장은 Interactions API지만, 기존 코드 전체가 generateContent 기반이라 일관성을 위해 계속 이 엔드포인트 사용).
- 이미지 생성 모델 후보 (전부 `gemini-2.5-flash-image`, `gemini-3.1-flash-image`, `gemini-3.1-flash-lite-image`, `gemini-3-pro-image`):
  - ⚠️ **최종 확인(2026-07-05, 공식 문서 재확인 완료): Gemini 이미지 생성 모델은 예외 없이 전부 무료 티어가 "Not available"이다.** 텍스트 모델과 달리 이미지 출력 기능은 결제(billing) 계정이 연결된 키가 아니면 어떤 모델도 호출 자체가 안 된다. 모델을 바꿔서 해결되는 문제가 아님.
  - **실수 기록 (2단계에 걸쳐 발생)**:
    1. 최초 구현 때 `gemini-3.1-flash-image`를 기본값으로 뒀다가, 사용자가 실제 키로 테스트하며 `429 ... free_tier_requests, limit: 0` 에러를 받음.
    2. 이를 "최신 모델만 유료, `gemini-2.5-flash-image`는 무료 티어 500회/일 가능"이라고 오판해 기본값을 `gemini-2.5-flash-image`로 바꿨는데, **이 판단의 근거가 blog.laozhang.ai / aifreeapi.com 같은 신뢰도 낮은 SEO 블로그였고 틀렸음.** 사용자가 다시 테스트하니 `gemini-2.5-flash-image`도 동일하게 `limit: 0` 에러 발생.
    3. `ai.google.dev/gemini-api/docs/pricing` 공식 문서를 WebFetch로 직접 확인해서야 "이미지 생성 모델 전체가 Not available(무료 티어 없음)"이라는 사실을 확정함.
  - **교훈**: Google API 요금/할당량처럼 사실관계가 중요하고 자주 바뀌는 정보는 검색 결과 요약(특히 SEO 블로그)을 그대로 믿지 말고, 반드시 `ai.google.dev` 공식 문서를 WebFetch로 직접 열어 원문을 확인할 것. 특히 "무료 티어 가능"처럼 사용자의 즉시 실행 가능 여부를 좌우하는 주장은 공식 출처 없이는 단정하지 말 것.
  - 현재 처리: 모델 기본값은 `gemini-2.5-flash-image`(균형)로 두되, UI에 "이미지 생성은 전부 결제 필요"라는 경고를 API 키 섹션과 이미지 옵션 섹션 두 군데에 상시 노출. `fetchGeminiRaw`가 `free_tier`+`limit: 0` 패턴을 감지하면 "모델을 바꿔도 소용없다"고 명확히 안내(더 이상 특정 모델로 바꾸라고 잘못 권하지 않음).
- 요청 형식: `generationConfig.responseModalities: ["TEXT","IMAGE"]`, 종횡비/해상도는 `generationConfig.imageConfig: {aspectRatio, imageSize}`.
  - 지원 aspectRatio: "1:1","3:4","4:3","9:16","16:9" 등. imageSize: "1K","2K","4K" (모델별 제약 다름 — flash-lite는 1K만).
- 참조 이미지 입력: `contents[].parts[]`에 `{"inlineData":{"mimeType":"image/png","data":"<base64>"}}` 형태로 함께 전달하면 이미지-투-이미지(일관성 유지)로 동작. 최대 14장까지 지원한다고 문서에 나오지만, 이 도구는 캐릭터 기준 이미지 1장만 사용.
- 응답 파싱: `candidates[0].content.parts`를 순회하며 `part.inlineData.data`(base64)를 찾음. `part.text`가 같이 올 수도 있음(모델의 설명 텍스트).

## 설계 결정 및 이유
1. **참조 이미지는 URL이 아니라 파일 업로드로 받는다.**
   이유: 브라우저 fetch로 임의의 외부 이미지 URL을 base64로 변환하려면 그 서버가 CORS를 허용해야 하는데, 대부분의 이미지 호스팅(디스코드 CDN 등)이 이를 허용하지 않음. `<input type="file">` + FileReader는 로컬 파일이라 CORS 제약이 없어 훨씬 안정적. (기존 MJ 탭의 `--cref` URL 입력 필드는 이번 도구에서 제거— 미드저니 전용 문법이라 더 이상 의미 없음.)
2. **생성된 이미지 원본(base64)은 localStorage 히스토리에 저장하지 않는다.**
   이유: localStorage는 보통 브라우저당 5~10MB 한도. 이미지 base64 여러 장을 넣으면 금방 초과되어 전체 데이터(캐릭터 시트, 프리셋 등)가 깨질 위험이 있음. 대신 히스토리에는 라벨/프롬프트/시간 등 메타데이터만 남기고, 실제 이미지는 생성 즉시 다운로드하거나 "폴더에 저장" 버튼으로 파일로 내보내도록 안내 문구를 명확히 넣는다.
3. **배치(표정/제스처/사물) 이미지 생성은 순차 처리 + 진행률 표시.**
   이유: 텍스트 배치처럼 한 번의 API 호출로 여러 개를 받을 수 없음(이미지는 항목당 1회 호출). 이미지 1장당 응답 시간이 텍스트보다 훨씬 길고(수 초~수십 초) 비용도 높으므로, 중간에 실패해도 이미 만든 이미지는 잃지 않도록 항목별로 즉시 화면에 표시 + 저장한다. 폴더 저장을 선택하면 배치 시작 시 폴더를 한 번만 고르고 그 핸들을 재사용해서 매 항목마다 다이얼로그가 뜨지 않게 한다.
4. **모델/해상도/종횡비는 드롭다운으로 선택 가능하게 하되, 기본값은 균형점인 `gemini-2.5-flash-image` + 1K + 1:1로 둔다.** 이미지 생성 자체가 결제 필요라는 사실은 모델별 라벨이 아니라 섹션 상단의 상시 경고 문구로 안내(모델과 무관하게 공통이라서). 배치 생성 시 이미지 개수가 많아질수록 비용이 커진다는 경고 문구를 배치 카운트 힌트 옆에 추가.
5. **API 호출 실패 시 재시도/에러 메시지 로직(`fetchGeminiRaw`, `extractGeminiText` 등)은 기존 코드를 그대로 재사용.** 다만 이미지 파트 추출은 텍스트 추출과 다르므로 별도 헬퍼(`extractGeminiImage`)를 추가.
6. **캐릭터 시트/스타일/배치 선택 UI, 프리셋, 백업/복원, 히스토리·즐겨찾기 구조는 그대로 유지.** MJ/GPT 플랫폼 탭과 그 전용 옵션(--cref, --cw, --ar, --no, GPT 비율/텍스트/참조 안내)만 "Gemini 이미지 설정" 패널로 교체.
7. **Etsy 상품 설명 생성 기능은 그대로 유지** (텍스트 전용이라 변경 불필요).

## 미해결/추후 검토
- 정확한 과금 단가는 문서에서 확인하지 못함 — 사용자가 자신의 Google AI Studio 결제 대시보드에서 실제 비용을 주기적으로 확인해야 함(도구 내 안내만 제공, 실시간 과금 조회는 미지원).
- 미드저니 연동(반자동 어시스턴트)은 이번 스코프에서 제외. 필요 시 별도 요청.
- Google AI Studio 결제 등록은 사용자가 맨 마지막 단계로 보류하기로 함(2026-07-05). 결제 전이라도 텍스트 생성(캐릭터시트/아이디어 다듬기/컨셉 리스트/Etsy)은 정상 동작하므로 그 전까지는 UI/레이아웃 다듬기에 집중.

## STEP 순서/레이아웃 재정비 (2026-07-05)
사용자가 PC에서 직접 사용해보고 "STEP 순서가 헷갈린다"고 피드백. 원인 분석 및 수정 내용:
1. **결과가 너무 멀리 있었음**: 기존엔 STEP 1(설정+생성 버튼)을 누르면 결과가 STEP 2(배치 생성 섹션 전체) + 제작 현황판을 지나야 나오는 STEP 3에 표시됐음 — 사용자가 결과를 못 찾고 헷갈릴 수 있는 가장 큰 원인으로 판단. **"생성된 이미지" 섹션을 STEP 1 바로 다음으로 이동**하고 번호를 STEP 2로 변경. 기존 배치 생성 섹션은 STEP 3으로 번호만 변경(내용 위치는 그대로 — 어차피 STEP 1 바로 뒤에 있었으므로 순서상 자연스럽게 3번째가 됨).
2. **필수 흐름 중간에 끼어있던 관리용 섹션**: "프리셋 관리"와 "데이터 백업·복원"이 STEP 0-2와 STEP 1 사이에 끼어 있어서, 번호 없는 전체 폭 패널이라 사용자가 이것도 순서대로 해야 하는 필수 단계로 착각할 여지가 있었음. 두 섹션을 그대로(내용 변경 없이) **페이지 맨 아래(Etsy 다음, footer 앞)로 이동** — 설정을 다 마친 뒤 "저장해두고 싶을 때" 쓰는 부가 기능이라는 성격에 더 맞음.
3. **사용 순서 안내박스(guide-box) 텍스트도 새 순서에 맞춰 갱신**: 0-1 → 0-2 → 1(아이디어&설정) → 2(결과 확인, 신규) → 3(배치, 기존 2번) → 4(Etsy) → "＋"(프리셋/백업, 맨 아래로 이동했다고 명시).
4. 이동 작업은 순수 HTML 마크업 위치 변경 + `<h2>`의 step-badge 텍스트만 수정. id/버튼/로직은 전혀 건드리지 않음 — `getElementById` 참조 전수 검증(104개 호출, 누락 0) 및 `node -e`로 `<script>` 문법 검증 통과, 브라우저 프리뷰에서 콘솔 에러 없음, `section.panel > h2` 순서를 JS로 추출해 최종 순서 확인 완료.

## STEP 1을 "레퍼런스 이미지 생성" ↔ "일괄 생성" 2탭으로 분리 (2026-07-05)
사용자가 "레퍼런스 이미지 생성 프로세스"와 "생성된 레퍼런스 이미지를 활용하는 프로세스"를 명확히 나눠달라고 요청. 기존엔 STEP 1(설정+단일생성)과 STEP 3(배치생성)이 별도 섹션으로 나뉘어 있었지만, 배치 생성이 사용하는 "기준 이미지"는 여전히 사용자가 파일을 수동으로 첨부해야만 적용되고, STEP 1에서 방금 생성한 결과 이미지를 자동으로 재사용하는 연결 고리가 없었음 — 이 부분이 실제 "활용" 프로세스의 핵심 공백이었음.

**구조 변경**: STEP 1(이미지 아이디어&설정) + STEP 2(생성된 이미지) + STEP 3(배치 생성)를 하나의 `STEP 1 "이미지 생성"` 패널로 합치고, 그 안에 상단 탭 2개(`#genTabRow`, 기존 `.tab-row`/`.tab-btn` 클래스 재사용)로 분리:
- **탭 "1. 레퍼런스 이미지 생성"** (`#genTabRefPane`): 아이디어·스타일·모델/비율/해상도·기준 이미지 업로드·생성 버튼·결과 이미지(다운로드/폴더저장/다시생성)까지 한 흐름에 포함.
- **탭 "2. 표정·제스처·사물 일괄 생성"** (`#genTabBatchPane`, 기존 STEP 3 내용 그대로): 모드 선택 칩·배치 생성.

**핵심 로직 추가**: `generatedRefImage` 전역 변수 신설(기존 `refImageData`는 수동 업로드 파일 전용으로 그대로 유지). 탭1에서 생성 성공 시(`renderResultImage`) 결과 이미지의 base64를 `generatedRefImage`에 자동 저장. `generateImageFromPrompt(promptText, refData)`에 두 번째 인자를 추가해 호출부가 명시적으로 참조 이미지를 지정할 수 있게 함 — 탭1의 단일 생성은 기존대로 인자 생략(내부적으로 `refImageData` 사용, 즉 수동 업로드만 반영), 탭2의 배치 생성(`generateBatchImages`, `regenerateBatchCard`)은 `generatedRefImage || refImageData`를 명시적으로 전달해 "방금 생성한 레퍼런스 이미지"를 우선 사용하고, 없으면 수동 업로드로 폴백.

**UI 피드백**: 탭2 상단에 `#batchRefStatus` 힌트를 추가해 현재 어떤 기준 이미지가 적용될지(✅생성된 이미지 사용 / 📎업로드 파일 사용 / ⚠️없음) 항상 보여줌. 탭 전환·생성 성공·수동 업로드 변경/제거 시마다 `updateBatchRefStatus()` 호출로 갱신.

**번호 재정리**: STEP 2/3이 탭 안으로 흡수되면서 Etsy 섹션을 STEP 4 → STEP 2로 재조정. 안내 박스(사용 순서)도 "①레퍼런스 생성 → ②일괄 생성" 흐름을 설명하도록 갱신.

**검증**: `node -e` 문법 검사, `getElementById` 참조 전수 검증(107개 호출, 누락 0) 통과. 브라우저에서 실제 탭 클릭(JS `.click()` 트리거 방식 — preview_click 툴 좌표 클릭이 이 세션에서 간헐적으로 씹혀서 우회) → 탭 전환/화면 표시 전환/`batchRefStatus` 문구 정상 확인. 가짜 1x1 PNG base64로 `renderResultImage()`를 직접 호출해 `generatedRefImage` 자동 설정 및 탭2 문구가 "✅ 방금 생성한 이미지 사용"으로 바뀌는 것까지 확인 완료. API 키 없이 생성 버튼 클릭 시 에러 처리도 기존과 동일하게 동작 확인.

## 2탭 → 3탭으로 재확장: "부가기능" 탭 신설 (2026-07-05)
사용자 피드백: "탭1에는 기준 이미지 생성에 필요한 옵션만 남기고, 나머지는 다음 단계로 넘겨야 실용적"이라며 기존 2탭 구조([레퍼런스 생성]/[일괄 생성])가 사실상 예전 STEP 2/3를 탭에 몰아넣은 것뿐이라 아쉽다는 지적. 요청한 최종 형태:
- 탭1: 기준 이미지 생성에 필요한 옵션만
- 탭2: 생성된 기준 이미지로 표정/제스처/사물/상황 조합 일괄 생성
- 탭3: Etsy 및 상품용 제작 옵션, 데이터 백업 등 부가기능

**변경 내용**: `#genTabRow`에 세 번째 탭 버튼(`data-gentab="extra"`, "3. 부가기능") 추가. 기존에 페이지 하단/별도 STEP2로 흩어져 있던 **Etsy 상품 설명 생성 + 프리셋 관리 + 데이터 백업·복원**을 전부 `#genTabExtraPane` 하나로 모음(각각 `border-top` 구분선으로만 구획, 별도 박스 중첩 없음 — 기존 코드 스타일 유지). Etsy 헤딩에서 `STEP 2` 배지 제거(이제 탭3 안의 항목이라 최상위 스텝 번호가 필요 없음).

**제작 현황판도 탭2 안으로 이동**: 표정/제스처/사물 진행 상황은 일괄 생성과 직접 관련된 정보라, 배치 컨트롤·결과 그리드 바로 아래(탭2 내부)로 옮김. 즐겨찾기·기록은 탭 구분과 무관하게 항상 참고할 수 있어야 해서 탭 바깥, 워크스페이스 패널 다음에 그대로 유지(탭1이든 탭2든 상관없이 생성 기록을 볼 수 있어야 하므로).

**남은 최상위 STEP 번호는 0-1, 0-2, 1(탭 3개 포함)뿐** — Etsy가 탭3으로 흡수되면서 별도의 "STEP 2" 배지가 필요 없어짐. 안내 박스(사용 순서)도 1/2/3 항목이 그대로 탭 1/2/3 순서와 대응하도록 재작성.

**JS 변경**: `els.genTabExtraPane` 참조 추가, 탭 클릭 핸들러가 3개 pane(`ref`/`batch`/`extra`)을 상호 배타적으로 토글하도록 확장. id/로직은 전혀 건드리지 않고 마크업 위치만 이동.

**검증**: 문법 검사 통과, `getElementById` 참조 108개 전수 검증(누락 0), 중복 id 없음 확인. 브라우저에서 탭1→2→3 전환 시 각 pane의 `display` 값이 상호 배타적으로 정확히 바뀌는지 확인, 탭2의 제작현황판 chip이 정상 렌더링(24개)되는지 확인, 탭3에서 프리셋 저장→목록 반영→삭제까지 실제 클릭으로 왕복 테스트해 정상 동작 확인. 콘솔 에러 없음.

## 사용 순서 가이드를 좌측 플로팅 패널로 전환 (2026-07-05)
사용자가 "어느 탭에서 작업하든 확인 가능하도록" 요청. 기존 `.guide-box`(본문 최상단 인라인 블록)를 `position:fixed; left:0; width:250px; height:100vh`로 바꾸고, `#guideToggleBtn`(☰ 사용순서 / ✕ 닫기)으로 열고 닫을 수 있게 함. 상태는 `mj_guide_open` 키로 localStorage에 저장되어 새로고침해도 유지. 1400px 이상 화면에서는 `.wrap`에 `margin-left:270px`를 줘서 본문과 겹치지 않게 처리(그 미만 화면에서는 열었을 때 본문 위에 겹쳐서 뜨는 오버레이 방식 — 필요하면 토글로 닫으면 됨).

**모바일 회귀 발견 및 수정**: 사용자가 "이 파일 모바일에서도 되냐"고 물어봐서 실제로 375px 뷰포트로 리사이�즈해 확인해보니, 이 사이드바가 기본값 "열림"으로 시작해서 좁은 화면의 66.7%를 가리는 문제를 발견함(직전 커밋에서 반응형 고려 없이 추가한 회귀). `setGuideOpen(safeLS.get('mj_guide_open') !== '0')` 단순 초기화 로직을 "저장된 선택이 있으면 그대로 따르고, 없으면(처음 여는 경우) 화면 폭이 768px 이상일 때만 기본 열림"으로 수정. 383px/1280px 두 뷰포트에서 각각 collapsed:true/false로 정확히 갈리는 것을 실제 리사이즈+새로고침으로 확인. 탭 3개 라벨(1./2./3.)은 375px에서 96px씩 3분할되어 텍스트가 3줄로 줄바꿈되지만 가로 스크롤은 발생하지 않음(허용 가능한 수준으로 판단, 별도 조치 안 함).
